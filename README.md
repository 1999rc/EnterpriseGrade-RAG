# EnterpriseGrade-RAG

An enterprise-grade Retrieval-Augmented
Generation (RAG) system 
using local embeddings and modular pipelines.

## Features 
- Local sentence-transformer embeddings (no API
keys)
- PDF ingestion and chuking 
- Vector store indexing 
- RAG retrieval + generation pipeline 

## Project Structure


## Setup 
```bash 
python3 -m venv venv
source .venv/bin/activate 
pip install -r requirements.txt